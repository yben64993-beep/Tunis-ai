# Firebase service account key (paste the content of serviceAccountKey.json here)
# It's a large JSON object, so it's better to load it from a file or a variable.
# For example, you can encode it in base64 and then decode it in the code.
# Or you can just paste the whole JSON string here.
SERVICE_ACCOUNT_KEY_BASE64=

# Gmail credentials
GMAIL_USER=tunisiamindai@gmail.com
GMAIL_PASS=

# OpenRouter API Key
OPENROUTER_KEY=
